#include <stdio.h>

int main()
{
    int number;
    printf("check the number is even or odd");
    scanf("%d",&number);
    if(number%2==0)
    {
        printf("the number is even");
    }
    else
    {
        printf("the number is odd");
    }
return 0;
}
